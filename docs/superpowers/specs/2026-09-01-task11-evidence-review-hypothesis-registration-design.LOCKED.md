# Task 11 — Evidence Review & Hypothesis Registration — Spec Lock Record

## Final status

```text
Status = FINAL LOCKED
Human Review = PASS
```

## Canonical Spec binding

Task / Spec title:

```text
Task 11 — Evidence Review & Hypothesis Registration Design Spec
```

Exact Spec path:

```text
docs/superpowers/specs/2026-09-01-task11-evidence-review-hypothesis-registration-design.md
```

Exact Spec commit SHA:

```text
7a3553770ea51e4ae72662fa44907f507779d22d
```

Exact Spec blob SHA:

```text
99e93ef6ca7cf1038561e7d6c4217e226ba99dfb
```

Exact Spec parent SHA:

```text
0a780ca95c4e6853bb2530436c6045c54f508e80
```

The human-approved Spec bytes identified by the exact path, commit SHA, and
blob SHA above are immutable. This Lock Record does not rewrite, amend, or
replace the approved Spec.

Any future change to the Spec requires a new explicit human review with a PASS
result and a new lock record that binds the newly approved path, commit, blob,
and parent SHAs. This Lock Record cannot authorize altered Spec bytes.

## Authorization state at lock

```text
Task 11 Design Spec                 = FINAL LOCKED
Human Review                       = PASS
Plan                               = NOT YET AUTHORIZED
Implementation                     = NOT AUTHORIZED
Ablation                           = NOT AUTHORIZED
Causal Replay                      = NOT AUTHORIZED
Score / Threshold / Ranking        = NOT AUTHORIZED
Feature Removal / Selection        = NOT AUTHORIZED
```
